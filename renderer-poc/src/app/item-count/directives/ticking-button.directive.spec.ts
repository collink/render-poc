import { TickingButtonDirective } from './ticking-button.directive';

describe('TickingButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new TickingButtonDirective();
    expect(directive).toBeTruthy();
  });
});
